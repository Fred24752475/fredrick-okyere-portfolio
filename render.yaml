services:
  - type: web
    name: fredrick-okyere-portfolio
    runtime: static
    plan: free
    branch: master
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
